<!doctype html>
<html lang="uk">  <!-- Define the document type and set the language to Ukrainian -->
<head>
    <meta charset="UTF-8">  <!-- Set the character encoding to UTF-8 -->
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">  <!-- Viewport settings for responsive design -->
    <meta http-equiv="X-UA-Compatible" content="ie=edge">  <!-- Ensure compatibility with Internet Explorer -->
    <!-- Include Bootstrap CSS from a CDN with integrity check for security -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Include custom CSS for common styles -->
    <link href="/css/common.css" rel="stylesheet">
    <!-- Include custom CSS for login styles -->
    <link href="/css/login.css" rel="stylesheet">
    <title>Реєстрація</title>  <!-- Set the title of the page to "Registration" -->
</head>
<body>

<div class="container-fluid h-100">  <!-- Full-height container -->
    <div class="row h-100">  <!-- Full-height row -->

        <!-- Side panel for larger screens -->
        <div class="side-panel col-0 col-md-4 px-4 d-none d-md-block">
            <h1>
                Вітаємо, користувачу!  <!-- Welcome message -->
            </h1>
            <p>
                Зареєструйтеся для вивчення нових неймовірних пес патрон знань  <!-- Registration prompt -->
            </p>
        </div>

        <!-- Main content area for both small and larger screens -->
        <div class="col-12 col-md-8 d-flex justify-content-center align-items-center">

            <div class="w-50">  <!-- Set the width of the form container to 50% of its parent -->
                <!-- Registration form with POST method -->
                <form action="register" method="POST">
                    <!-- Username input field -->
                    <div class="mb-3">
                        <label for="userName" class="form-label">Ім'я користувача</label>
                        <input type="text" class="form-control" name="userName" id="userName" aria-describedby="Ім'я користувача" autocomplete="name">
                    </div>
                    <!-- Email input field -->
                    <div class="mb-3">
                        <label for="userEmail" class="form-label">E-mail користувача</label>
                        <input type="email" class="form-control" name="userEmail" id="userEmail" aria-describedby="E-mail користувача" autocomplete="email">
                    </div>
                    <!-- Password input field -->
                    <div class="mb-3">
                        <label for="password" class="form-label">Пароль</label>
                        <input type="password" class="form-control" name="password" id="password" autocomplete="new-password">
                    </div>
                    <!-- Confirm password input field -->
                    <div class="mb-3">
                        <label for="confirmPassword" class="form-label">Підтвердіть пароль</label>
                        <input type="password" class="form-control" name="confirmPassword" id="confirmPassword" autocomplete="new-password">
                    </div>

                    <!-- Submit button -->
                    <button type="submit" class="btn btn-primary">Зареєструватися</button>
                </form>
            </div>

        </div>
    </div>

</div>

<!-- Include Bootstrap JS from a CDN with integrity check for security -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
